[pytest]
# Test files in this course are named "*-tests.py" (not the default "test_*.py"),
# so tell pytest to discover them. importlib import mode lets pytest import the
# hyphenated example/test filenames cleanly.
python_files = *-tests.py
addopts = --import-mode=importlib
